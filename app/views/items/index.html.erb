<div>
 <%= link_to "登録", new_item_path %>
</div>

<h1>登山地一覧</h1>
 <% @items.each do |item| %>
 <div>
    <p>山名</p>
    <span><%= link_to item.name, item_path(item.id) %></span>
 </div>
  <%= attachment_image_tag item, :image, :fill, 100, 100, format: 'jpeg' %>
 <% end %>
